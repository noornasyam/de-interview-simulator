# Git Certification Questions

Store objective Git questions here, such as `mcq` and `fill_blank`.

Use this folder for commits, branches, merges, rebases, pull requests, tags, and conflict resolution.
