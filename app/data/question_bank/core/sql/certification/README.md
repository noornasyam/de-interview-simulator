# SQL Certification Questions

Store objective SQL questions here, such as `mcq` and `fill_blank`.

Use this folder for cloud-neutral SQL fundamentals, joins, aggregations, window functions, query order, and optimization concepts.
